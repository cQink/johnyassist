"""Внешние коннекторы (Azure Vision, Face++, social-analyzer, переводчик, …).

Правила общие для всех (см. current_work_plan.md §5):
  - приватность и согласие — для face/OSINT каждый вызов требует явной команды;
  - rate-limit — суточная квота хранится в state.json и переживает перезапуск;
  - кеширование — ответ не меняется сутками, повторный платный запрос незачем;
  - офлайн-режим — падение сервиса, сети или отсутствие ключа дают честный
    отказ с причиной, а не исключение посреди разговора.

Общее (кеш с TTL, квота на диске, warn_once/cooldown из http_client) лежит в
store.py — копировать его в каждый коннектор значило бы чинить один баг
четырнадцать раз.
"""

from . import base, store

__all__ = ["base", "store"]
