# Текущий план работы над Johnny

## 1. База и устойчивость
- Укрепить архитектуру обработки команд.
- Разделить маршрутизацию, выполнение и генерацию ответа.
- Улучшить обработку ошибок, отмены и таймаутов.
- Сохранить и расширить тестовое покрытие.

## 2. Контекст и осознанность
- Добавить более сильную память диалога и фактов.
- Сделать систему «осведомлённой» о текущем состоянии: ожидание, выполнение, пауза, ошибка.
- Улучшить поведение в повторяющихся сценариях общения.

## 3. Расширяемость
- Сделать код более модульным и удобным для добавления новых действий.
- Вынести обработку действий в более явный слой, чтобы проще подключать новые интеграции.
- Упростить поддержку новых команд и сценариев.

## 4. Голос, автономность и вайбкод
- Подготовить основу для персонализированного голоса.
- Начать работу над самовосполнением и автоматизацией повторяющихся действий.
- Подготовить почву для более «умного» поведения и инициативности.

## 5. Интеграции и анализ среды
- Расширять интеграции с внешними сервисами и библиотеками.
- Постепенно добавлять контекст из окружения, безопасности и мультимодальности.
- Добавить OCR и live-распознавание из камеры для расширенного контекстного анализа.
- Добавить возможность селективного скриншотинга объектов на экране, а не только full-screen, с последующим OCR и распознаванием текста/элементов.
- Добавить поддержку чтения социальных сетей и мессенджеров: сбор потоков, выделение ключевых участников, анализ стиля и смысла диалогов, автоматическое черновое формирование ответов (auto-reply) с фильтрацией и безопасностью.
- Сделать ассистента полезным в большинстве сфер работы на ПК: печать, кодинг прямо в файле, взаимодействие через промпт в Visual Studio Code и других IDE, управление окнами и макросами.
- Добавить поддержку работы с другими ИИ: прокси промптов, объединение вариантов и контекстное применение в редакторах.
- Добавить анализ проекта и git-потока: структура, зависимости, TODO, ветки, коммиты и резюме изменений.
- Организовать документацию как общий ресурс, разделённый на блоки: `docs/pentest/`, `docs/arduino/`, `docs/<тематика>/` с сабблоками и тематическими знаниями.
- Делать систему более полезной в реальной повседневной жизни.
 - Добавить поддержку внешних инструментов обратного поиска и распознавания: интеграция с TinEye (reverse image search) и FaceCheck.ID (face recognition), с опциями приватности, rate-limit и кэширования результатов. — ЗАМЕНЕНО владельцем на Azure AI Vision и Face++. Обратного поиска и поиска человека по лицу в итоге нет ни у одного коннектора (Azure не индексирует интернет, Face++ по сети не ходит) — вместо них «что на картинке», OCR, сравнение двух лиц и разбор лица в кадре. Приватность, rate-limit и кеш выполнены как требовалось. Подробности — в строках 69–70.
 - Интегрировать дополнительные внешние тулзы и фреймворки для мультимодальной обработки, авторинга и OSINT:
	 - Netryx-Astra-V2 (OSINT pipeline и агрегаторы данных)
	 - VoiceStudio ([debpalash/VoiceStudio]) — локальная/облачная студия для голосовых моделей
	 - antvis/Infographic — генерация инфографики из данных + визуализация
	 - zhouxiaoka/autoclip — автоматическая генерация хайлайтов и нарезок видео
	 - calesthio/OpenMontage — агентная видео-продакшн платформа (pipelines, skills)
	 - qeeqbox/social-analyzer — поиск и анализ профилей в соцсетях
	 - ORB-HD/deface — видео-анонимизация лиц для приватности
	 - MoonMonet/Translator — локальный/десктопный переводчик для UX и локализации
	 - nexu-io/html-video — генерация видео из HTML/CSS/data локально
 - Для каждого интегратора предусмотреть:
	 - требования по приватности, согласию и модерации (особенно для face/OSINT)
	 - rate-limiting, кэширование и офлайн-режимы
	 - простой коннектор с опциональной локальной установкой или облачным endpoint

## Текущий todo list

- [x] Inspect current architecture
- [x] Stabilize test environment
- [x] Expand coverage for core flows
- [x] Refactor for extensibility — [details/refactor_for_extensibility.md](docs/superpowers/plans/details/refactor_for_extensibility.md) → [docs/plugins.md](../../plugins.md)
- [x] Add context and memory enhancements — [details/add_context_and_memory_enhancements.md](docs/superpowers/plans/details/add_context_and_memory_enhancements.md)
- [x] Prepare voice and autonomy roadmap — [details/prepare_voice_and_autonomy_roadmap.md](docs/superpowers/plans/details/prepare_voice_and_autonomy_roadmap.md) → [docs/voice_and_autonomy_roadmap.md](../../voice_and_autonomy_roadmap.md)
- [x] Integrate external voice model service (NVIDIA NeMo / nemotron-labs-voicechat) — [details/integrate_external_voice_model_service.md](docs/superpowers/plans/details/integrate_external_voice_model_service.md) → [services/tts-server/README.md](../../../services/tts-server/README.md)
- [x] Add OCR and live camera recognition support — [details/add_ocr_and_live_camera_recognition_support.md](docs/superpowers/plans/details/add_ocr_and_live_camera_recognition_support.md) — распознавание текста и объектов работает через `azure-vision`, картинку Джони теперь берёт и сам: `johnny/capture.py` снимает экран (все мониторы), **выделенную мышью область**, активное окно или кадр с веб-камеры. Голосом «прочитай что на экране», «прочитай выделенное», «что вот здесь», «что видит камера», «запомни что на экране»; мышкой — две карточки со списком источника. Живая проверка 2026-08-08: OCR прочитал вкладки браузера, описание сцены отработало через обход региона без caption, выделенная область прочитана вживую («Сезон 1 Серия 7») и описана тегами.
  - Снимок удаляется сразу после ответа (`capture.discard` в `finally`) — на нём бывает чужая переписка; до удаления он лежит в `models/captures/`, а `models/` целиком в .gitignore. Отправку наружу по-прежнему решает согласие у коннектора, захват его не обходит.
  - Селективный скриншот — `capture.region()` + `johnny/region_select.py`. Выделение живёт в ОТДЕЛЬНОМ ПРОЦЕССЕ: панель держит свой Tk-цикл в своём потоке, а тулкиты выполняются в фоновом, и Tk из чужого потока вешает панель молча, без исключения. Экран снимается ДО показа оверлея, иначе полупрозрачная плёнка попала бы в кадр; промежуточный полный снимок удаляется в `finally` — человек выделил кусок именно потому, что остальное отправлять не хотел.
  - Свёрнутое окно спрашиваем у `win32gui.IsIconic`, а не считаем по размеру: Windows отдаёт для него прямоугольник 160×28 за границей экрана — размер правдоподобный, проверка «слишком маленькое» его пропускала, и OCR отвечал «текста нет» вместо честного отказа. Найдено тестом.
  - В долгую память само по себе не пишется ничего — только по команде «запомни что на экране»: факты живут до «забудь» и уезжают в промпт каждой модели. Результат распознавания идёт в короткую память, чтобы следом работало «переведи это».
  - Локальный OCR (Tesseract/EasyOCR) из шагов не делался сознательно — причина в details-файле.
- [x] Переделать интерфейс панели — решение владельца 2026-08-09: «сейчас он
  выглядит топорно… как ребёнок на трудах делал… надо буквально сделать новый
  интерфейс, прям приоритет». Сделано: `johnny/ui_theme.py` (новый) + переписан
  `johnny/panel.py`.
  - **Диагноз, а не вкусовщина.** Панель заливала 40% окна насыщенным
    фиолетовым (`#818CF8`) и клала сверху плоские цветные прямоугольники
    `tk.Button`. Самое яркое пятно экрана ничего не означало — отсюда «бумага
    на бумаге». Правило новой палитры: **цвет это СИГНАЛ, а не площадь.**
    Корпус тёмный и молчит; лаймовый горит только там, где идёт живой сигнал;
    фиолетовый понижен до второго цвета данных (модель, устройство). Оба цвета
    из макетов владельца остались — сменилась их РОЛЬ, а не сами цвета.
  - Глубина в Tk делается не тенями (их нет), а разницей яркости соседних
    поверхностей и рамкой в 1px. Отсюда три уровня: `WELL` (утоплено) →
    `CHASSIS` (фон) → `PANEL` (приподнято), значения намеренно близки.
  - **Кнопки нарисованы на Canvas** (`ui_theme.Button`), а не `tk.Button`:
    у `tk.Button` не бывает скруглений и рамки в пиксель, и именно поэтому
    старые кнопки выглядели аппликацией. Там же `Slider` (у `tk.Scale` ручка —
    объёмный брусок из Windows 95) и `Tabs`.
  - Выпадающие списки — `tk.OptionMenu`, а НЕ `ttk.Combobox`, и это не вкус:
    кнопку со стрелкой у Combobox рисует тема clam, цвет ей задать нечем, и
    она остаётся светло-серым квадратом — единственным светлым пятном на
    тёмной панели. Проверено вживую, скриншотом. Родной индикатор OptionMenu
    убран (`indicatoron=False`) и заменён «птичкой»: без неё поле выбора
    неотличимо от поля ввода.
  - **Статус стал главным**: колодец осциллографа во всю ширину (состояние
    словом, живая волна, движок, последняя команда). Настройки уехали во
    вкладки ГОЛОС / РАСПОЗНАВАНИЕ / ТУЛКИТЫ. Раньше и статус, и настройки
    лежали в одном скролле, и до статуса надо было доскроллить.
  - Нулевая линия в колодце — не украшение: в покое волна плоская, и без
    линии колодец читался пустой коробкой. У осциллографа без сигнала линия
    тоже остаётся.
  - Шрифты: **Bahnschrift SemiCondensed** (DIN, шрифт приборных панелей;
    стоит в Windows 10+) для подписей и **Cascadia Mono** для данных. Segoe UI
    в 9–10 пунктах на подписях превращался в кашу.
  - `visualizer.frame()` получил `disc_size`: размеры волны и кружка были
    одним числом, и крупная волна тянула кружок на пол-колодца. `BARS` 12→28 —
    волна идёт через весь колодец. Все прежние тесты visualizer'а прошли без
    правок (они про поведение, а не про числа).
  - Ловушка, стоившая отладки: `self._w` в наследнике `tk.Canvas` — это
    СЛУЖЕБНОЕ имя виджета в tkinter. Присвоение туда ширины ломает каждый
    вызов Tcl («invalid command name»). Ширина/высота теперь `_cw`/`_ch`.
  - Проверено: 1012 passed. `tests/test_face_index_connector.py` не
    собирается — на этой машине нет `cv2`; это НЕ следствие правок интерфейса
    (модуль лиц не трогали), а тот же перенос проекта, из-за которого нет и
    `~/.insightface`.
- [x] Волна в колодце — круглые концы и затухание к краям (2026-08-09).
  Столбики рисуются линией с `capstyle="round"`, яркость гаснет к краям через
  `ui_theme.blend`: волна сходит на нет, а не обрывается на границе колодца.
  - **Кружок остался ПИКСЕЛЬНЫМ — это решение владельца, не недоделка.**
    По просьбе «хочу реалистичнее, не просто пиксели» была сделана светящаяся
    сфера (ореол из 20 колец + затемнение к краю + блик к источнику света);
    посмотрев вживую, владелец выбрал вернуть пиксельную стилизацию из макетов,
    оставив новую волну. Код сферы удалён, чтобы не лежал мёртвым; `blend`
    оставлен — на нём держится затухание волны.
  - Что стоит знать, если сферу захотят вернуть: в Tk нет ни прозрачности, ни
    градиентов, поэтому свечение — это заранее посчитанные сплошные цвета,
    смешанные с ФОНОМ. И порядок слоёв обязателен: **свет → волна → тело**.
    Кольца ореола непрозрачны, и нарисованные после волны они выедают вокруг
    шара тёмную проплешину (поймано скриншотом, не тестом).
- [x] Починен живой баг: на «как у тебя дела» Джони отвечал «Не расслышал,
  повторите» (2026-08-09).
  - Причина НЕ в распознавании — в `history.log` фраза записана верно
    («как дела [слитно/groq]»). Виноват промпт: фраза «не расслышал, повторите»
    стояла в нём ПРИМЕРОМ стиля обращения, и Llama 70B копировала её дословно
    как готовый ответ. Воспроизведено вживую до правки.
  - Правило на будущее: пример реплики годится в промпт, только если его
    нельзя выдать за ответ на произвольный вопрос. «Открываю браузер» привязано
    к действию и попугаем не звучит; «не расслышал, повторите» подходит куда
    угодно. Два сторожа в `tests/test_brain.py`.
  - Заодно исправлена вторая половина: модель объясняла, что это не команда,
    вместо ответа. В пункт 4 промпта добавлено «ПРОСТО ОТВЕТЬ НА ВОПРОС».
  - Попутно выяснено живьём: цепочка запасных моделей на этой машине РАБОТАЕТ —
    Groq упёрся в лимит 429, и ответ пришёл от `claude`. Это расходится с
    записью ниже (строка про «программы claude не установлено»): она верна для
    прежней машины, здесь `claude` есть в `%APPDATA%\npm`.
- [x] Add PC workflow support — [details/add_pc_workflow_support.md](docs/superpowers/plans/details/add_pc_workflow_support.md).
  **СУЖЕН решением владельца 2026-08-09** («согласен, сузить всё до одного») и
  сделан в суженном виде: `johnny/git_tools.py` + `johnny/actions/git_action.py`.
  - Из трёх частей пункта две отпали ПО ФАКТУ, а не по лени, и это стоит
    назвать вслух, иначе кто-нибудь заведёт их заново:
    - **Управление окнами уже было.** `launch_app`, `app_focus`, `move_window`,
      `launch_on_monitor` в `johnny/actions/windows.py` плюс
      `find_window_by_exe`. Фокус любой программы работает обобщённо: появится
      IDE — одна строка в `apps.yaml`, и «фокус код» заработает без кода.
    - **Прокси промптов в редакторы = следующий пункт плана дословно.** Шаг 2
      исходной спеки («отправить контекст файла во внешний ИИ и применить
      правку») — это и есть «Add external AI prompt proxy support in editors».
      Делать дважды по двум спекам незачем; вся работа там.
  - **IDE на машине владельца нет вообще** (ни VS Code, ни JetBrains —
    проверено). Прописать в `apps.yaml` путь к несуществующему редактору было
    бы тем же, за что выше ругается пункт про TinEye: обещать невыполнимое
    хуже, чем не иметь команды. Поэтому записи про IDE нет.
  - **Границы git-слоя — главное в нём, и они закреплены тестами:**
    - `git add` не вызывается ВООБЩЕ. `git add -A` голосом — способ отправить
      в коммит то, чего человек не видел: свежий `secrets.yaml`, дамп, лог с
      чужой перепиской. Что войдёт в коммит, человек выбирает заранее руками;
      пустой индекс Джони так и называет. Сторож —
      `test_commit_never_stages_anything_itself`.
    - `push` не реализован вовсе: необратим и выносит код наружу, а команда
      может сработать от ослышки. Сторож — `test_there_is_no_push`.
    - Никакого `shell=True`: сообщение коммита приходит из распознавания речи,
      то есть снаружи. Сторож — `test_no_public_helper_runs_a_shell`.
    - `git_workdir` пуст по умолчанию, и при пустом git НЕ запускается. Иначе
      он выполнялся бы в папке старта Джони и рассказывал бы про чужой
      репозиторий — незаметно для человека.
  - Разбор идёт по `--porcelain`: без него git отвечает на языке системы, и на
    русской локали разбор развалился бы.
  - **Баг, который нашёлся только живым прогоном** (фейковый раннер его не
    ловил): на свежем `git init` команда `log` отдаёт НЕНУЛЕВОЙ код, и Джони
    зачитывал английское `fatal: your current branch 'main' does not have any
    commits yet`. Пустой репозиторий — не сбой; добавлен параметр `soft`
    в `_guarded` и тест.
  - Русские числительные («1 файл / 2 файла / 5 файлов») — свои, в
    `git_tools.plural`: ответы звучат ГОЛОСОМ, и «3 файл» на слух спотыкает.
    `morph.py` не подошёл, он про сопоставление имён.
  - Проверено вживую на настоящем репозитории, весь путь от фразы до git:
    статус/ветка/ветки/изменения/коммиты читаются верно, голосовой коммит
    создал реальный коммит, а неотслеживаемый файл остался нетронутым.
    46 новых тестов, набор целиком — 1058 passed.
- [ ] Add external AI prompt proxy support in editors — [details/add_ai_prompt_proxy_support_in_editors.md](docs/superpowers/plans/details/add_ai_prompt_proxy_support_in_editors.md)
- [ ] Add project context analysis and smart workspace automation — [details/add_project_context_analysis_and_automation.md](docs/superpowers/plans/details/add_project_context_analysis_and_automation.md)
- [ ] Add social media ingestion and analysis (feeds, key users, style analysis) — [details/add_social_media_ingestion_and_analysis.md](docs/superpowers/plans/details/add_social_media_ingestion_and_analysis.md)
- [ ] Add auto-reply engine (draft, review, send) with moderation & safety — [details/add_auto_reply_engine.md](docs/superpowers/plans/details/add_auto_reply_engine.md)
- [ ] Integrate social platforms connectors (X/Twitter, Telegram, Discord, Facebook, VK) — [details/integrate_social_platforms_connectors.md](docs/superpowers/plans/details/integrate_social_platforms_connectors.md)
- [x] Integrate TinEye reverse-image search connector — [details/integrate_tineye_connector.md](docs/superpowers/plans/details/integrate_tineye_connector.md) → [docs/connectors.md](../../connectors.md) — ЗАМЕНЕНО по решению владельца на **Azure AI Vision** (`johnny/connectors/azure_vision.py`). Это НЕ равнозначная замена, и подмену важно не пропустить: TinEye отвечал «где ещё в интернете встречается эта картинка», Azure — «что на картинке изображено и что на ней написано». Поиска по индексу интернета у Azure нет вовсе, у Microsoft он был (Bing Visual Search) и закрыт. Поэтому команда «откуда эта картинка» убрана из commands.yaml, а не переехала: обещать невыполнимое хуже, чем не иметь команды. Взамен закрыт пункт про OCR — см. строку выше.
- [x] Включить коннекторы на живых ключах и проверить их вживую — ключи владельца в `config/secrets.yaml` (файл в `.gitignore`), блок `connectors` в `settings.yaml` раскомментирован, `azure-vision.endpoint` = `https://kamiyevh-resource.cognitiveservices.azure.com`.
  - Azure: `read`, `tags`, `objects` работают. `caption` в **swedencentral** отвечает `400 not supported in this region` — а он первый в `DESCRIBE_FEATURES`, и Azure отвергает весь запрос целиком, то есть «что на картинке» не работала бы вообще. Коннектор снимает недоступную фичу (имя читает из сообщения) и переспрашивает один раз; результат помнит в модуле, потому что фабрика пересобирает коннектор на каждый вызов. Живой ответ: «Вижу на картинке (по-английски): person, grass, soccer player, sport, football», OCR — «Прочитал: unicef WEEKmedia».
  - Альтернатива обходу: создать ресурс Vision в регионе с `caption` (eastus,
    westeurope) — тогда описание вернётся связной фразой, а не списком слов.
    ~~Решено владельцем 2026-08-08: ресурс пересоздаётся.~~ **Отменено
    2026-08-08: Azure не даёт сменить регион, «функция временно недоступна».**
    Значит обход в `azure_vision.py` из временной страховки стал ПОСТОЯННЫМ
    механизмом — снимать его нельзя ни при каких обстоятельствах, иначе «что на
    картинке» отвалится целиком: `caption` первый в `DESCRIBE_FEATURES`, а
    Azure отвергает запрос целиком из-за одной неподдерживаемой фичи.
    Практическая цена решения: описание остаётся списком слов («person, grass,
    soccer player»), связной фразы на этом ресурсе не будет. OCR, tags и
    objects работают полностью и от региона не зависят.
    Менять `endpoint`/`azure_vision_key` не нужно — ресурс остаётся прежним.
  - Face++: `detect` и `compare` проверены живьём («Лиц на снимке: 1. Возраст навскидку 26, выражение — neutral»; «Сходство 41.66 при пороге 73.97 — скорее разные люди»). Журнал доступа пишет только время, режим, имя файла, короткий хеш и число лиц. `image_url` на этом ключе отвечает `INVALID_IMAGE_URL` — коннектор им и не пользуется, снимок уходит файлом.
  - Согласие на лица сначала было оставлено выключенным намеренно (ключ значит «могу», включать биометрию должен человек); проверено, что при этом оба действия отказывают внятно и в сеть не ходят. **2026-08-08 владелец включил его: `faceplusplus.consent: true`.** Отдельной строкой, а не общим `consent`, — выключить одни лица, оставив картинки и профили, по-прежнему можно одним словом.
  - Попутно закрыта дыра: пустой путь в `_opened()` давал `Path(".")`, то есть каталог — он проходил все проверки, а `open()` падал «Permission denied: '.'», и человек слышал errno вместо «снимок не назван». Два теста.
- [x] Integrate FaceCheck.ID face-recognition connector with consent/moderation — [details/integrate_facecheck_connector.md](docs/superpowers/plans/details/integrate_facecheck_connector.md) → [docs/connectors.md](../../connectors.md) — ЗАМЕНЕНО по решению владельца на **Face++** (`johnny/connectors/faceplusplus.py`). Подмена ещё существеннее: FaceCheck искал лицо по всему интернету, Face++ по сети не ходит — он сравнивает ровно те два снимка, которые ему дали, и разбирает лицо в кадре. Команда «найди это лицо» убрана. Согласие и журнал доступа остались: лицо всё равно уезжает третьей стороне. Пол не запрашивается вовсе, вердикт — по строгому порогу из ответа (1e-5) и всегда произносится вместе со сходством.
- [ ] ВОЗМОЖНО ВЕРНУТЬ TinEye и FaceCheck.ID — решение владельца, не закрыто. Замена выше сняла не всё: вопрос «откуда эта картинка» и вопрос «кто этот человек» остались без исполнителя, и ни один наш коннектор на них не отвечает. Если эти два вопроса понадобятся, вернуть придётся именно TinEye и FaceCheck.ID — аналогов с поиском по индексу интернета у Azure и Face++ нет.
  - Возврат дешевле, чем кажется: `johnny/connectors/base.py` не менялся, вся политика (согласие, кеш, квота, пауза, офлайн-отказ) живёт в нём, а коннектору остаются `available()` и `_run()`. В git истории нет (проект не под git), но обе реализации восстановимы по деталям ниже и по docs/connectors.md.
  - Что придётся вернуть вместе с коннектором, иначе он недосягаем: фразы «откуда эта картинка» / «найди это лицо» в `config/commands.yaml`, действия `reverse_image` / `face_search` в `johnny/actions/connector_action.py`, ключи `tineye_api_key` / `facecheck_api_token` в `config/secrets.yaml.example`, строки в `BUILDERS` (`johnny/connectors/factory.py`) и плитки в `johnny/panel_tools.py`.
  - Сторож: `tests/test_connector_actions.py::test_removed_phrases_are_gone_from_commands` падает, если `reverse_image` или `face_search` вернутся. Это не препятствие, а напоминание — тест удаляется тем же решением, которым возвращается команда, осознанно и в одном коммите.
  - Возвращать их ВМЕСТО Azure/Face++ не нужно: вопросы разные и не пересекаются. TinEye не скажет, что на картинке написано, Azure не скажет, где картинка встречается. Держать оба — нормально, лишний ключ ничего не стоит, пока согласие выключено.
- [x] Локальный индекс лиц (InsightFace) + Face++ судьёй — [details/integrate_local_face_index.md](docs/superpowers/plans/details/integrate_local_face_index.md). Решение владельца 2026-08-08: «Face++ использовать как сравнения исходного лица и найденного через альтернативу PimEyes». **СДЕЛАНО 2026-08-09:** `johnny/connectors/face_index.py` (вектор и косинус, `requires_consent = False`), индекс в gitignored `models/face-index/` (`vectors.npy` + `paths.json`, 2 КБ/лицо), команда пополнения `python -m johnny.connectors.face_index <путь>`, фразы «есть ли в архиве *», действие `search_face_archive`. Живой прогон на сэмплах insightface: пустой архив → пополнение → незнакомец не найден → найден после добавления → групповая фотография переспросила. 44 новых теста.
  - Границу назвать вслух, иначе повторим историю с TinEye→Azure: локальный индекс ищет по **своему** архиву, а не по интернету. В пустом индексе он находит ноль. Вопрос «кто этот незнакомец» это не закрывает — его закрывает пункт про FaceCheck (строка 90), и никак иначе. «Найди это лицо» в командах нет и не будет — сторож `test_archive_phrases_promise_only_the_archive`.
  - Первым шагом — `min_interval_seconds` у Face++: сейчас 1.0, а живая проверка ключа даёт `CONCURRENCY_LIMIT_EXCEEDED` при паузе меньше 8–12 с. Это нужно независимо от пункта и заодно ограничивает число кандидатов на второй этап. Следствие, найденное при реализации: второй этап зовётся **только для сомнительного кандидата** (между 0.28 и 0.40), а не для верхних пяти — при паузе 10 с пять сравнений это минута молчания, а уверенный косинус 0.40+ уже ответ.
  - FaceSet/Search (1:N) на ключе владельца проверен живьём и доступен, но индекс на нём строить не надо: Face++ не отдаёт векторов, только непрозрачный `face_token`, — весь архив тогда живёт на их серверах и офлайна нет вовсе.
  - Загрузка модели ~~4.1 с~~ — замер был неверен, см. спеку: `import onnxruntime` 24–25 с + конструкция 1.3 с + `prepare` 1.0 с, снимок 0.8 с. Поэтому `available()` проверяет пакет через `find_spec`, а не импортом (иначе 25 с тишины в голосовом пути), и рядом есть `prewarm()` — те же 26 с, но в фоновом потоке. Вопрос, цеплять ли его к старту, — владельцу.
- [x] «Доп мозг» по API-ключу: Opus 5 или GPT-5.6 Sol — [details/integrate_api_strong_brain.md](docs/superpowers/plans/details/integrate_api_strong_brain.md). Решение владельца 2026-08-08: «Будем делать обращение по апи ключу клауд опус 5 либо гпт 5.6 сол и они будут как доп мозг. Позже сделаю локальную ИИшку и она будет заменять опус 5 либо 5.6 гпт». Этим же решением снят NeMo (см. закладку ниже). Слой моделей уже был устроен под это: `make_providers()` отдаёт список пар `(имя, функция)`, побеждает первый непустой ответ — новая модель это функция `prompt -> str`, и больше ничего.
  - Слот ОДИН и сменный (`strong_brain: "" | opus | gpt`), а не две модели разом: опрашивать обеих — двойная цена за один ответ, а не двойная надёжность. Локальная модель встанет в таблицу `_STRONG_BRAINS` одной строкой, цепочку править не придётся.
  - Позиция в цепочке = политика расходов: **Groq → сильная модель → `claude -p`**. За Groq она включается только там, где Groq не справился, — это и есть «доп». `strong_brain_first: true` делает её основной, отдельным переключателем и осознанно.
  - Форму запроса нельзя копировать из `brain_groq`, и каждая ловушка молчаливая: `temperature` на Opus 5 отвечает 400, `max_tokens: 200` уходит на размышление целиком и возвращает пустой оплаченный ответ, `stop_reason: "refusal"` приходит с HTTP **200** и пустым `content` (то есть `content[0]` падает там, где сервис ответил нормально). У GPT-5.6 Sol потолок называется `max_completion_tokens`. На каждую — тест.
  - Ключ и настройка разведены как у коннекторов: `strong_brain` значит «хочу», ключ в `secrets.yaml` — «могу». Оплата ключа не должна сама начинать траты на каждую фразу.
  - **Работает вживую с 2026-08-08**: владелец дал ключ Opus 5, `strong_brain: "opus"`. Ключ не от `api.anthropic.com`, а от посредника **agentrouter.org**, поэтому добавлена настройка `strong_brain_base_url` (адрес не секрет и живёт в `settings.yaml`, как endpoint у Azure; ключ — в `secrets.yaml`).
    - Посредник смотрит не только на ключ, но и на КЛИЕНТА: сырой POST тем же ключом отбивается «unauthorized client detected», официальный SDK проходит. Прежнее решение «здесь SDK, а не наш `http_client`» стало обязательным — переписать модуль на свой клиент теперь нельзя.
    - `_rejected` и `_clients` помнят ПАРУ `(ключ, адрес)`, а не ключ: один и тот же ключ на официальном адресе отвечает 401, а на своём работает — запомнив ключ, мы выключили бы и рабочую пару.
  - **Замер задержки 2026-08-08** (промпт на 227 команд, `effort: low`): обычно **2–7 с**, но первые вызовы после паузы — **29–39 с**. До первого слова уходит почти всё это время, генерация занимает 0.5–2 с, поэтому стримингом ожидание не разбить: во время паузы приходить нечему.
    - Потолок оставлен на 20 с: молчать 40 секунд хуже, чем сказать «не понял» и дать переспросить, тем более что повторный вызов почти всегда быстрый.
    - Зато один таймаут больше **не уводит модель в 60-секундный cooldown**. Он задумывался как защита от пропавшего интернета при наличии запасного пути, а тут глушил единственный живой мозг ровно тогда, когда маршрутизатор прогрелся. Первый таймаут прощаем, второй подряд — уже сбой (`TIMEOUTS_BEFORE_COOLDOWN`), иначе вернулись бы к трате 20 секунд на каждую фразу.
  - **Запасного пути на этой машине нет, и это выяснилось только сейчас.** Цепочка была `['opus', 'claude']`, но программы `claude` на машине владельца не установлено — `brain_claude.run` возвращал пустоту мгновенно и молча, то есть страховка за сильной моделью была фикцией. Теперь `_find_claude()` возвращает пустую строку вместо голого имени (`CreateProcess` PATHEXT не применяет, так что надежда «а вдруг запустится» была строго слабее `which`), а `run()` один раз говорит об этом в лог. Модуль до сих пор не был покрыт тестами вовсе — теперь покрыт.
- [ ] Integrate Netryx-Astra-V2 OSINT connector — [details/integrate_netryx_astra_v2.md](docs/superpowers/plans/details/integrate_netryx_astra_v2.md)
- [ ] Integrate VoiceStudio (voice model studio) — [details/integrate_voicestudio.md](docs/superpowers/plans/details/integrate_voicestudio.md)
- [ ] Integrate antvis/Infographic (infographic generation) — [details/integrate_infographic.md](docs/superpowers/plans/details/integrate_infographic.md)
- [ ] Integrate zhouxiaoka/autoclip (video clipping/highlight) — [details/integrate_autoclip.md](docs/superpowers/plans/details/integrate_autoclip.md)
- [ ] Integrate calesthio/OpenMontage (agentic video production) — [details/integrate_openmontage.md](docs/superpowers/plans/details/integrate_openmontage.md)
- [x] Integrate qeeqbox/social-analyzer (profile analysis) — [details/integrate_social_analyzer.md](docs/superpowers/plans/details/integrate_social_analyzer.md) → [docs/connectors.md](../../connectors.md)
- [ ] Integrate ORB-HD/deface (video anonymization) — [details/integrate_deface.md](docs/superpowers/plans/details/integrate_deface.md)
- [x] Integrate MoonMonet/Translator (desktop translator) — [details/integrate_translator.md](docs/superpowers/plans/details/integrate_translator.md) — DONE: `johnny/connectors/translator.py`, действие `translate`. Сам MoonMonet — Tauri-приложение с треем и без программного API, вызвать его из голосовой команды нечем; взято требование «локальная обработка предпочтительна»: офлайн-движок argostranslate, запасной путь — LibreTranslate (на нём же собран, поэтому ответы совпадают). Адрес пустой по умолчанию — в сеть сам не идёт.
- [ ] Integrate nexu-io/html-video (programmatic HTML->video) — [details/integrate_html_video.md](docs/superpowers/plans/details/integrate_html_video.md)
- ~~cryptpad, Saltcorn, Wiki.js~~ — **сняты с плана решением владельца 2026-08-08.**
  Все три — самостоятельные веб-приложения, которые надо где-то развернуть и
  держать; голосом с них снимается разве что «открой и создай страницу», то есть
  ассистент выступает кнопкой «открыть браузер». Не заводить заново без нового
  решения. Спеки `details/integrate_cryptpad.md`, `integrate_saltcorn.md`,
  `integrate_wikijs.md` остались на диске и потеряли силу — по ним не работать.

## Где остановились (2026-08-08)

Закладка для следующего захода — по порядку, сверху вниз.

1. ~~Прогнать `pytest` после установки `insightface`.~~ **Сделано 2026-08-08:
   870 passed.** Разведка по лицам ставила `insightface`, и он поднял
   `opencv-python` с 4.x до 5.0.0.93 плюс семь пакетов; на проект это не
   повлияло, откатывать окружение не нужно.
2. ~~**Дальше — NeMo на Google Cloud**, так решил владелец («мы начнём с немо»).~~
   **Снято 2026-08-08 решением владельца:** «значит немо мы откидываем, я думал
   он может работать как доп мозг развёрнутый на сервере. можешь удалить».
   Клон `external/nemo-speech` удалён. Роль «доп мозга» вместо развёрнутой на
   сервере модели закрыта обращением по API-ключу — см. пункт про Opus 5 /
   GPT-5.6 Sol выше, он **сделан**.
   Работа по TTS-сервису при этом НЕ отменена и к NeMo не привязана: голос
   по-прежнему нужен, и единственный жизнеспособный русский путь — XTTS v2.
   Сделано ранее: клиентский `token` (Bearer), `http_client.get()`, `warm_up()`,
   проброс токена через speaker, проверка токена на `/synthesize` и `/health`.
   Осталось: `$PORT` для Cloud Run, `JOHNNY_TTS_TOKEN` в README, `.env` для
   запуска, тест на саму проверку токена. Развёртывание упирается в владельца:
   ни `gcloud`, ни `docker` на машине не установлены.
3. ~~**Владельцу должен ответ** на вопрос «что использовать в связке с Face++».~~
   **Закрыто 2026-08-08.** Решение владельца: «Face++ использовать как сравнения
   исходного лица и найденного через альтернативу PimEyes». Записано пунктом
   плана (строка 87) и спекой `details/integrate_local_face_index.md`. Замеры
   разведки не пропали: InsightFace `buffalo_l` (512-мерный вектор, косинус двух
   разных людей 0.026 при пороге ~0.35–0.40, загрузка 4.1 с, снимок 0.8–1.5 с на
   CPU) — локальный отбор, Face++ — судья по верхним кандидатам.
   `face_recognition`/`dlib` отпали: под 3.14 колёс нет, нужен MSVC, пакет стоит
   с 2020. `SpyScrap` спит с 2021, паттерн Social Mapper площадки блокируют.
   `deepkrak3n` про ники и почты — пересекается с готовым `social-analyzer`.
   Попутно поправлено то, что вылезло в разведке: `min_interval_seconds` у
   Face++ был 1.0, а живой ключ требует 8–12 с; поставлено 10.0, набор зелёный
   (870 passed).
   Граница остаётся в силе и в спеке названа первой строкой: «искать в своём
   архиве» и «устанавливать личность незнакомца» — разные вещи, второе это
   паттерн Clearview и биометрия по ст. 9 GDPR без согласия субъекта. Локальный
   индекс делает только первое; по умолчанию второго нет.

4. **Локальный индекс лиц сделан 2026-08-09** (пункт выше). Следующий пункт по
   порядку — «Add PC workflow support». FaceCheck по-прежнему не трогаем: это
   отдельное решение владельца и оно не принято.

Ключи Groq и fish вписаны в `config/secrets.yaml` владельцем 2026-08-09
(файл в `.gitignore`). Настройки под них в `settings.yaml` уже стояли
(`tts_provider: fish`, `fish_model_id`, `groq_model`) — менять было нечего.

**Сильная модель на этой машине НЕ подключена, хотя включена.** В
`settings.yaml` стоит `strong_brain: "opus"` («хочу»), но `anthropic_api_key`
в `secrets.yaml` пуст («не могу») — при переносе проекта ключ не приехал, и
цепочка сейчас `groq → claude`. Чтобы Opus заработал, нужно ТРИ вещи, и одной
только строки с ключом мало: `pip install anthropic` (пакет не установлен,
отсутствие видно только по строке в логе) + сам ключ + `strong_brain_base_url`,
СОВПАДАЮЩИЙ с происхождением ключа (сейчас там agentrouter.org; для
официального ключа с api.anthropic.com адрес надо очистить, иначе 401).
Отдельно: даже с ключом Opus не увидит кода, пока Groq первый в цепочке и
отвечает всегда — под это в план помощи с кодом заложен `code_assist_prefer_strong`
(см. `plans/2026-08-09-ai-code-assist.md`).

Открытые решения владельца: возвращать ли TinEye и FaceCheck.ID (строка 91);
`whisper_model` — `medium` (22.2 с на пятисекундном отрезке) или `small`
(5.5 с); цеплять ли `prewarm()` индекса лиц к фоновому потоку на старте (первый
поиск иначе стоит ~26 с); можно ли удалить дубль модели в
`~/.insightface/models/buffalo_l` (326 МБ, копия лежит в `models/buffalo_l`).
Закрыто 2026-08-08: `faceplusplus.consent` включён; cryptpad, Saltcorn
и Wiki.js сняты с плана; пересоздание ресурса Azure в регионе с `caption`
**отменено** — Azure не даёт сменить регион, обход в `azure_vision.py` стал
постоянным (подробности в строках 77–86).
